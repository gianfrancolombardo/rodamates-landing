---
import { 
  Truck, 
  Heart, 
  MapPin, 
  Package, 
  Globe, 
  ArrowRight, 
  Users, 
  Leaf, 
  Zap, 
  Camera, 
  MessageCircle, 
  CheckCircle,
  Instagram,
  ArrowUpRight,
  X,
  Check
} from 'lucide-astro';
import SwipeMockup from './SwipeMockup.astro';
import Logo from './Logo.astro';
---

<div class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 overflow-hidden">
  <!-- Header -->
  <header class="fixed top-0 left-0 right-0 z-50 bg-slate-900/80 backdrop-blur-xl border-b border-slate-700/50">
    <div class="max-w-6xl mx-auto px-6 py-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-3">
          <Logo class="w-8 h-8" color="text-purple-500" />
          <h1 class="text-xl font-bold bg-gradient-to-r from-white to-blue-200 bg-clip-text text-transparent">
            ReNomada
          </h1>
        </div>
        <nav class="hidden md:flex items-center space-x-8">
          <a href="#problema" class="text-slate-300 hover:text-white transition-colors">Problema</a>
          <a href="#solucion" class="text-slate-300 hover:text-white transition-colors">Solución</a>
          <a href="#como-funciona" class="text-slate-300 hover:text-white transition-colors">Cómo funciona</a>
        </nav>
      </div>
    </div>
  </header>

  <!-- Sección Hero -->
  <section class="min-h-screen flex items-center px-6 pt-20 relative pb-20 md:pb-0">
    <div class="max-w-6xl mx-auto w-full">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <!-- Contenido Izquierdo -->
        <div class="space-y-8">
          <div class="space-y-6">
            <div class="inline-flex items-center space-x-3 bg-blue-500/10 border border-blue-500/20 rounded-full px-3 py-2">
              <span class="w-2 h-2 bg-blue-400 rounded-full animate-pulse"></span>
              <span class="text-blue-300 text-sm font-medium">Próximamente</span>
            </div>
            
            <h1 class="heading-lg font-black leading-tight">
              <span class="text-white">Libera espacio. Reduce tu huella.</span>
              <br>
              <span class="bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">
                Ayuda a un viajero que tienes al lado.
              </span>
            </h1>
          </div>
          
          <p class="text-body text-slate-300 leading-relaxed max-w-lg">
            La app que une a la comunidad nómada a través de la economía circular. 
            <br/>Regala lo que ya no usas, encuentra lo que necesitas cerca de ti y deja una huella más positiva en cada kilómetro.
          </p>

          <!-- Formulario CTA -->
          <div id="cta-form" class="space-y-4 max-w-md">
            <p class="text-slate-400 text-sm">Únete al movimiento de la economía circular nómada. ¡Te avisamos en cuanto estemos en la carretera!</p>
            
            <form id="email-form" class="space-y-3">
              <div class="relative group">
                <input 
                  type="email" 
                  id="email-input"
                  placeholder="Tu correo electrónico" 
                  class="w-full px-4 py-3 rounded-xl border-2 border-slate-600 bg-slate-800/50 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500 focus:bg-slate-800 transition-all duration-300 group-hover:border-slate-500"
                  required
                >
                <div class="absolute inset-0 rounded-xl bg-gradient-to-r from-blue-500/20 to-purple-500/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
              </div>
              
              <button 
                type="submit"
                class="w-full bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white font-bold py-3 px-6 rounded-xl transition-all duration-300 transform hover:scale-[1.02] hover:shadow-xl hover:shadow-blue-500/25 flex items-center justify-center space-x-2"
              >
                <span>¡Avísame del lanzamiento!</span>
                <ArrowUpRight class="w-4 h-4" />
              </button>
            </form>

            <div class="relative">
              <div class="absolute inset-0 flex items-center">
                <div class="w-full border-t border-slate-600"></div>
              </div>
              <div class="relative flex justify-center text-sm">
                <!-- <span class="px-4 bg-slate-900 text-slate-400">o</span> -->
              </div>
            </div>

            <p class="text-slate-400 text-sm mb-3">
              Si no quieres dejarnos tu email, está bien. Solo queremos saber que te interesa.
            </p>
            
            <button 
              id="interest-btn"
              class="w-full border-2 border-slate-600 text-slate-300 hover:border-slate-500 hover:text-white font-semibold py-3 px-6 rounded-xl transition-all duration-300 hover:bg-slate-800/50 flex items-center justify-center space-x-2 group"
            >
              <span>Sí, me interesa</span>
              <Heart class="w-4 h-4 group-hover:scale-110 transition-transform duration-300" />
            </button>
          </div>

          <!-- Mensaje de Agradecimiento (Oculto) -->
          <div id="thank-you" class="hidden max-w-md">
            <div class="bg-gradient-to-r from-green-500/10 to-emerald-500/10 border border-green-500/20 rounded-xl p-4 text-center mb-10">
              <CheckCircle class="w-8 h-8 text-green-400 mx-auto mb-3" />
              <h2 class="text-xl font-bold text-white mb-2">¡Gracias por unirte a la tribu! ✨</h2>
              <p class="text-slate-300 text-sm">Te avisaremos en cuanto ReNomada esté en la carretera. ¡Buena ruta!</p>
            </div>
          </div>
        </div>

        <!-- Mockup de la App con Animación de Swipe Real -->
        <SwipeMockup />
      </div>
    </div>
  </section>

  <!-- Sección El Problema -->
  <section id="problema" class="section-padding bg-gradient-to-b from-slate-800 to-slate-900">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="max-w-3xl mx-auto text-center mb-20">
        <h2 class="heading-lg font-bold text-white mb-6">Vivir ligero es un arte. Y a veces, un desafío.</h2>
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 lg:gap-12">
        
        <div class="group p-8 bg-slate-800/50 border border-slate-700/50 rounded-2xl hover:border-slate-600/50 transition-all duration-500 hover:scale-[1.02] hover:shadow-xl hover:shadow-slate-900/50">
          <div class="w-16 h-16 bg-gradient-to-br from-green-500/20 to-green-600/20 border border-green-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Leaf class="w-8 h-8 text-green-400" />
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Tirar choca con tus valores</h3>
          <p class="text-slate-300 leading-relaxed">Ese objeto está en perfecto estado. Deshacerte de él genera un residuo innecesario y una sensación de culpa. Sabes que debe haber una alternativa mejor.</p>
        </div>

        <div class="group p-8 bg-slate-800/50 border border-slate-700/50 rounded-2xl hover:border-slate-600/50 transition-all duration-500 hover:scale-[1.02] hover:shadow-xl hover:shadow-slate-900/50">
          <div class="w-16 h-16 bg-gradient-to-br from-blue-500/20 to-blue-600/20 border border-blue-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Truck class="w-8 h-8 text-blue-400" />
          </div>
          <h3 class="heading-md font-bold text-white mb-4">El 'por si acaso' ocupa lugar</h3>
          <p class="text-slate-300 leading-relaxed">En tu casa con ruedas, cada centímetro es sagrado. Acumular objetos que ya no usas no solo te roba espacio, te roba libertad.</p>
        </div>
        
        
        
        <div class="group p-8 bg-slate-800/50 border border-slate-700/50 rounded-2xl hover:border-slate-600/50 transition-all duration-500 hover:scale-[1.02] hover:shadow-xl hover:shadow-slate-900/50">
          <div class="w-16 h-16 bg-gradient-to-br from-purple-500/20 to-purple-600/20 border border-purple-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <MapPin class="w-8 h-8 text-purple-400" />
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Tu mapa cambia cada día</h3>
          <p class="text-slate-300 leading-relaxed">Las apps de segunda mano no están hechas para una vida en movimiento. ¿De qué sirve encontrar algo a 50 km si mañana estarás en la otra punta del país?</p>
        </div>
        
        <div class="group p-8 bg-slate-800/50 border border-slate-700/50 rounded-2xl hover:border-slate-600/50 transition-all duration-500 hover:scale-[1.02] hover:shadow-xl hover:shadow-slate-900/50">
          <div class="w-16 h-16 bg-gradient-to-br from-orange-500/20 to-orange-600/20 border border-orange-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Package class="w-8 h-8 text-orange-400" />
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Paquetes sin destino</h3>
          <p class="text-slate-300 leading-relaxed">Sin una dirección fija, conseguir lo que necesitas se convierte en una misión imposible. Los envíos no entienden de libertad.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección La Solución - Rediseñada -->
  <section id="solucion" class="section-padding bg-white">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="text-center mb-20">
        <div class="inline-flex items-center space-x-2 bg-blue-500/10 text-blue-600 px-4 py-2 rounded-full mb-6">
          <Zap class="w-5 h-5" />
          <span class="font-semibold">La Solución</span>
        </div>
        
        <h2 class="heading-lg font-bold text-slate-900 mb-8 max-w-4xl mx-auto">
          Conectamos viajeros para darle una segunda vida a las cosas.
        </h2>
        
        <p class="text-body text-slate-600 leading-relaxed max-w-3xl mx-auto">
          ReNomada digitaliza la tradición nómada del intercambio. Una app que conecta viajeros cercanos para compartir recursos de forma instantánea y sostenible.
        </p>
      </div>
      
      <!-- Grid de características centrado -->
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8 max-w-5xl mx-auto">
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-green-500/20 to-green-600/20 border border-green-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <Globe class="w-10 h-10 text-green-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Reduce, Reutiliza, Rueda</h3>
          <p class="text-slate-600">Cada objeto que regalas o encuentras es un producto nuevo que no se fabrica. Menos CO₂, menos residuos, más conciencia.</p>
        </div>
        
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-blue-500/20 to-blue-600/20 border border-blue-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <ArrowRight class="w-10 h-10 text-blue-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Cerca es mejor</h3>
          <p class="text-slate-600">Nuestra geolocalización dinámica te muestra solo lo que tienes a tu alrededor. La solución más sostenible y comunitaria es siempre la más cercana.</p>
        </div>
        
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-purple-500/20 to-purple-600/20 border border-purple-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <Users class="w-10 h-10 text-purple-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Confianza de tribu</h3>
          <p class="text-slate-600">Una comunidad que comparte un estilo de vida. El intercambio se basa en el apoyo mutuo y en la confianza de tratar con alguien como tú.</p>
        </div>
        
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-pink-500/20 to-pink-600/20 border border-pink-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <MessageCircle class="w-10 h-10 text-pink-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Del maletero a tus manos</h3>
          <p class="text-slate-600">Sin envíos, sin intermediarios, sin costes. Todo se gestiona por chat para un encuentro simple, directo y en persona.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección Cómo Funciona -->
  <section id="como-funciona" class="section-padding bg-gradient-to-b from-slate-900 to-slate-800">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="max-w-3xl mx-auto text-center mb-20">
        <h2 class="heading-lg font-bold text-white mb-6">Así de simple, así de rápido</h2>
        <p class="text-body text-slate-300 leading-relaxed">Tres pasos para transformar tu experiencia nómada</p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-10 lg:gap-16">
        <div class="group text-center">
          <div class="relative mb-8">
            <div class="w-24 h-24 bg-gradient-to-br from-blue-500 to-blue-600 rounded-3xl flex items-center justify-center mx-auto group-hover:scale-110 transition-transform duration-300 shadow-xl shadow-blue-500/25">
              <Camera class="w-12 h-12 text-white" />
            </div>
            <div class="absolute -top-3 left-1/2 -translate-x-1/2 w-8 h-8 bg-white rounded-full flex items-center justify-center text-blue-600 font-bold text-sm shadow-lg border-2 border-blue-100">1</div>
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Publica lo que no usas</h3>
          <p class="text-slate-300 leading-relaxed">Toma una foto, añade descripción y ¡listo! Tu objeto está disponible para la comunidad.</p>
        </div>
        
        <div class="group text-center">
          <div class="relative mb-8">
            <div class="w-24 h-24 bg-gradient-to-br from-green-500 to-green-600 rounded-3xl flex items-center justify-center mx-auto group-hover:scale-110 transition-transform duration-300 shadow-xl shadow-green-500/25">
              <MapPin class="w-12 h-12 text-white" />
            </div>
            <div class="absolute -top-3 left-1/2 -translate-x-1/2 w-8 h-8 bg-white rounded-full flex items-center justify-center text-green-600 font-bold text-sm shadow-lg border-2 border-green-100">2</div>
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Encuentra lo que necesitas</h3>
          <p class="text-slate-300 leading-relaxed">Swipe por objetos cercanos en tiempo real. La geolocalización te muestra solo lo relevante.</p>
        </div>
        
        <div class="group text-center">
          <div class="relative mb-8">
            <div class="w-24 h-24 bg-gradient-to-br from-purple-500 to-purple-600 rounded-3xl flex items-center justify-center mx-auto group-hover:scale-110 transition-transform duration-300 shadow-xl shadow-purple-500/25">
              <MessageCircle class="w-12 h-12 text-white" />
            </div>
            <div class="absolute -top-3 left-1/2 -translate-x-1/2 w-8 h-8 bg-white rounded-full flex items-center justify-center text-purple-600 font-bold text-sm shadow-lg border-2 border-purple-100">3</div>
          </div>
          <h3 class="heading-md font-bold text-white mb-4">Conecta y coordina</h3>
          <p class="text-slate-300 leading-relaxed">Chat integrado para organizar el encuentro. Simple, directo y sin complicaciones.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección Impacto Ambiental -->
  <section class="section-padding bg-gradient-to-br from-emerald-50 to-teal-50">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="max-w-3xl mx-auto text-center mb-20">
        <h2 class="heading-lg font-bold text-slate-900 mb-6">
          Un pequeño gesto, un gran respiro para el planeta
        </h2>
        <p class="text-body text-slate-600 leading-relaxed">
          Cada objeto que reutilizas en lugar de comprar nuevo reduce significativamente tu huella ambiental. La economía circular nómada en acción.
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
        <div class="group bg-white p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-500 hover:scale-[1.02] border border-emerald-100">
          <div class="w-16 h-16 bg-gradient-to-br from-emerald-500/20 to-emerald-600/20 border border-emerald-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Globe class="w-8 h-8 text-emerald-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-4">Menos CO2</h3>
          <p class="text-slate-600 leading-relaxed">Evita la producción y transporte de nuevos productos. Cada objeto reutilizado ahorra emisiones.</p>
        </div>
        
        <div class="group bg-white p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-500 hover:scale-[1.02] border border-teal-100">
          <div class="w-16 h-16 bg-gradient-to-br from-teal-500/20 to-teal-600/20 border border-teal-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Leaf class="w-8 h-8 text-teal-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-4">Ahorro de Agua</h3>
          <p class="text-slate-600 leading-relaxed">Reduce el consumo de agua en la fabricación. La reutilización es la forma más eficiente de conservar recursos.</p>
        </div>
        
        <div class="group bg-white p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-500 hover:scale-[1.02] border border-green-100">
          <div class="w-16 h-16 bg-gradient-to-br from-green-500/20 to-green-600/20 border border-green-500/30 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <Heart class="w-8 h-8 text-green-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-4">Menos Residuos</h3>
          <p class="text-slate-600 leading-relaxed">Objetos útiles no van al vertedero. Cada intercambio es una victoria para la gestión de residuos.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección La Comunidad -->
  <section class="section-padding bg-white">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="text-center mb-20">
        <div class="inline-flex items-center space-x-2 bg-purple-500/10 text-purple-600 px-4 py-2 rounded-full mb-6">
          <Users class="w-5 h-5" />
          <span class="font-semibold">La Comunidad</span>
        </div>
        
        <h2 class="heading-lg font-bold text-slate-900 mb-8 max-w-4xl mx-auto">
          Más que una app, somos una tribu rodante
        </h2>
        
        <p class="text-body text-slate-600 leading-relaxed max-w-3xl mx-auto">
          La comunidad nómada es nuestra razón de ser. Conectamos personas que comparten valores de libertad, sostenibilidad y apoyo mutuo.
        </p>
      </div>
      
      <!-- Grid de valores centrado -->
      <div class="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto">
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-emerald-500/20 to-emerald-600/20 border border-emerald-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <Leaf class="w-10 h-10 text-emerald-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Sostenibilidad Real</h3>
          <p class="text-slate-600">Valores que se traducen en acciones concretas cada día</p>
        </div>
        
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-blue-500/20 to-blue-600/20 border border-blue-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <Heart class="w-10 h-10 text-blue-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Apoyo Mutuo</h3>
          <p class="text-slate-600">Una red de ayuda donde todos dan y reciben</p>
        </div>
        
        <div class="text-center group">
          <div class="w-20 h-20 bg-gradient-to-br from-purple-500/20 to-purple-600/20 border border-purple-500/30 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <MessageCircle class="w-10 h-10 text-purple-600" />
          </div>
          <h3 class="heading-md font-bold text-slate-900 mb-3">Conexiones Auténticas</h3>
          <p class="text-slate-600">Amistades reales que nacen de necesidades compartidas</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección CTA Final -->
  <section class="section-padding bg-gradient-to-br from-slate-800 to-slate-900">
    <div class="max-w-4xl mx-auto container-padding text-center">
      <h2 class="heading-lg font-bold text-white mb-8">
        Únete a la tribu que comparte recursos y cuida del camino.
      </h2>
      
      <p class="text-body text-slate-300 mb-12 leading-relaxed">
        Únete a la revolución nómada sostenible. Tu próxima aventura empieza con un swipe.
      </p>
      
      <div id="cta-final" class="space-y-4 max-w-md mx-auto">
        <form id="email-form-final" class="space-y-3">
          <div class="relative group">
            <input 
              type="email" 
              id="email-input-final"
              placeholder="Tu correo electrónico" 
              class="w-full px-4 py-3 rounded-xl border-2 border-slate-600 bg-slate-800/50 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500 focus:bg-slate-800 transition-all duration-300 group-hover:border-slate-500"
              required
            >
            <div class="absolute inset-0 rounded-xl bg-gradient-to-r from-blue-500/20 to-purple-500/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
          </div>
          
          <button 
            type="submit"
            class="w-full bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white font-bold py-3 px-6 rounded-xl transition-all duration-300 transform hover:scale-[1.02] hover:shadow-xl hover:shadow-blue-500/25 flex items-center justify-center space-x-2"
          >
            <span>¡Avísame del lanzamiento!</span>
            <ArrowUpRight class="w-4 h-4" />
          </button>
        </form>

        <div class="relative">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-slate-600"></div>
          </div>
          <div class="relative flex justify-center text-sm">
            <!-- <span class="px-4 bg-slate-800 text-slate-400">o</span> -->
          </div>
        </div>

        <p class="text-slate-400 text-sm text-center mb-3">
          Si no quieres dejarnos tu email, está bien. Solo queremos saber que te interesa.
        </p>
        
        <button 
          id="interest-btn-final"
          class="w-full border-2 border-slate-600 text-slate-300 hover:border-slate-500 hover:text-white font-semibold py-3 px-6 rounded-xl transition-all duration-300 hover:bg-slate-800/50 flex items-center justify-center space-x-2 group"
        >
          <span>Sí, me interesa</span>
          <Heart class="w-4 h-4 group-hover:scale-110 transition-transform duration-300" />
        </button>
      </div>

      <div id="thank-you-final" class="hidden max-w-md mx-auto mt-8">
        <div class="bg-gradient-to-r from-green-500/10 to-emerald-500/10 border border-green-500/20 rounded-xl p-4 text-center">
          <CheckCircle class="w-8 h-8 text-green-400 mx-auto mb-3" />
          <h2 class="text-xl font-bold text-white mb-2">¡Gracias por unirte a la tribu! ✨</h2>
                        <p class="text-slate-300 text-sm">Te avisaremos en cuanto ReNomada esté en la carretera. ¡Buena ruta!</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-12 bg-slate-900 border-t border-slate-800">
    <div class="max-w-6xl mx-auto container-padding">
      <div class="flex flex-col md:flex-row items-center justify-between space-y-6 md:space-y-0">
        <div class="flex items-center space-x-3">
          <Logo class="w-8 h-8" color="text-purple-500" />
          <div>
            <h3 class="text-lg font-bold text-white">ReNomada © 2025</h3>
            <p class="text-slate-400 text-sm">Libera espacio. Reduce tu huella.</p>
          </div>
        </div>
        
        <div class="flex items-center space-x-6">
          <a href="#" class="text-slate-400 hover:text-white transition-colors group">
            <div class="w-8 h-8 bg-slate-800 hover:bg-slate-700 rounded-xl flex items-center justify-center transition-colors group-hover:scale-110">
              <Instagram class="w-4 h-4" />
            </div>
          </a>
        </div>
      </div>
    </div>
  </footer>
</div>

<script>
  // Google Analytics 4 - Configuración básica
  // Reemplaza GA_MEASUREMENT_ID con tu ID real
  const GA_MEASUREMENT_ID = 'G-0GLRK3PH8L';
  
  // Función para enviar eventos a GA4
  function sendGAEvent(eventName: any, parameters: any = {}) {
    if (typeof (window as any).gtag !== 'undefined') {
      (window as any).gtag('event', eventName, parameters);
    }
  }

  // Función para enviar datos a Web3Forms
  async function sendToWeb3Forms(data: any) {
    try {
      const formData = new FormData();
      
      // Campos requeridos por Web3Forms
      formData.append('access_key', 'e0e38729-90bf-46eb-a0bc-36bc5ce8c887'); // 🔑 Pega tu API key aquí
      formData.append('subject', 'Nuevo lead de ReNomada');
      
      // Datos del usuario
      formData.append('email', data.email || 'meinteresa@renomada.com');
      formData.append('type', data.type || 'interest');
      formData.append('location', data.location || 'hero_section');
      formData.append('message', `Usuario ${data.type === 'email' ? 'con email' : 'solo interés'} desde ${data.location}`);
      
      const response = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        body: formData
      });
      
      const result = await response.json();
      
      if (result.success) {
        console.log('Datos enviados correctamente a Web3Forms');
        return true;
      } else {
        console.error('Error al enviar datos a Web3Forms');
        return false;
      }
      
    } catch (error) {
      console.error('Error enviando a Web3Forms:', error);
      return false;
    }
  }



  // Manejo del formulario principal
  document.getElementById('email-form')?.addEventListener('submit', async function(e) {
    e.preventDefault();
    const emailInput = document.getElementById('email-input') as HTMLInputElement;
    const email = emailInput?.value;
    
    if (email) {
      // Enviar evento a GA4
      sendGAEvent('email_submitted', {
        email: email,
        location: 'hero_section'
      });
      
      // Enviar a Web3Forms
      const success = await sendToWeb3Forms({
        email: email,
        type: 'email',
        location: 'hero_section'
      });
      
      if (success) {
        // Mostrar mensaje de agradecimiento
        document.getElementById('cta-form')?.classList.add('hidden');
        document.getElementById('thank-you')?.classList.remove('hidden');
      }
    }
  });

  // Manejo del formulario final
  document.getElementById('email-form-final')?.addEventListener('submit', async function(e) {
    e.preventDefault();
    const emailInput = document.getElementById('email-input-final') as HTMLInputElement;
    const email = emailInput?.value;
    
    if (email) {
      // Enviar evento a GA4
      sendGAEvent('email_submitted', {
        email: email,
        location: 'final_section'
      });
      
      // Enviar a Web3Forms
      const success = await sendToWeb3Forms({
        email: email,
        type: 'email',
        location: 'final_section'
      });
      
      if (success) {
        // Mostrar mensaje de agradecimiento
        document.getElementById('cta-final')?.classList.add('hidden');
        document.getElementById('thank-you-final')?.classList.remove('hidden');
      }
    }
  });

  // Manejo del botón de interés principal
  document.getElementById('interest-btn')?.addEventListener('click', async function() {
    // Enviar evento de interés a GA4
    sendGAEvent('interest_expressed', {
      location: 'hero_section',
      action: 'button_click'
    });
    
          // Enviar a Web3Forms
      const success = await sendToWeb3Forms({
        type: 'interest',
        location: 'hero_section'
      });
    
    if (success) {
      // Mostrar mensaje de agradecimiento
      document.getElementById('cta-form')?.classList.add('hidden');
      document.getElementById('thank-you')?.classList.remove('hidden');
    }
  });

  // Manejo del botón de interés final
  document.getElementById('interest-btn-final')?.addEventListener('click', async function() {
    // Enviar evento de interés a GA4
    sendGAEvent('interest_expressed', {
      location: 'final_section',
      action: 'button_click'
    });
    
          // Enviar a Web3Forms
      const success = await sendToWeb3Forms({
        type: 'interest',
        location: 'final_section'
      });
    
    if (success) {
      // Mostrar mensaje de agradecimiento
      document.getElementById('cta-final')?.classList.add('hidden');
      document.getElementById('thank-you-final')?.classList.remove('hidden');
    }
  });

  // Tracking de página vista
  document.addEventListener('DOMContentLoaded', function() {
    sendGAEvent('page_view', {
      page_title: 'ReNomada Landing',
      page_location: window.location.href
    });
    

  });

  // Smooth scrolling para navegación
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e: Event) {
      e.preventDefault();
      const href = (this as HTMLAnchorElement).getAttribute('href');
      if (href) {
        const target = document.querySelector(href);
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      }
    });
  });
</script>

<!-- Google Analytics 4 -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-0GLRK3PH8L"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-0GLRK3PH8L');
</script>
