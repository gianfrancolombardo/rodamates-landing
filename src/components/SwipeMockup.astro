---
// Componente SwipeMockup para el hero de ReNomada
---

<div class="flex justify-center lg:justify-end">
  <div class="relative group">
    <!-- Efecto de brillo -->
    <div class="absolute -inset-1 bg-gradient-to-r from-blue-600 to-purple-600 rounded-[2rem] blur opacity-25 group-hover:opacity-40 transition duration-1000 group-hover:duration-200"></div>
    
    <!-- Phone frame (card height == phone height) -->
    <div class="relative w-[320px] h-[580px] rounded-[2.2rem] bg-white border border-10 border-black/80 shadow-2xl overflow-hidden">
      
      <!-- Screen area with padding; allow swipe to overflow the padded content -->
      <div class="absolute inset-0 p-3 overflow-visible">
        <article class="auto-swipe relative w-full h-full rounded-[2.2rem] overflow-hidden bg-white">
          <!-- Product image: Camping Chair -->
          <img
            src="https://plus.unsplash.com/premium_photo-1681488389277-0dcaa6bb35a5?q=80&w=880&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
            alt="Silla de camping"
            class="absolute inset-0 w-full h-full object-cover"
          />

          <!-- Subtle bottom gradient for legibility of title/subtitle -->
          <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/35 via-black/10 to-transparent"></div>

          <!-- GREEN overlay: right swipe mood -->
          <div class="overlay-like absolute inset-0 flex items-center justify-center">
            <span class="label-like text-2xl md:text-3xl font-bold tracking-wide uppercase text-green-700 font-heading">¡Si lo quiero!</span>
          </div>

          <!-- RED overlay: left swipe mood -->
          <div class="overlay-nope absolute inset-0 flex items-center justify-center">
            <span class="label-nope text-2xl md:text-3xl font-bold tracking-wide uppercase text-rose-700 font-heading">No, gracias</span>
          </div>

          <!-- Title + subtitle (glass pill, bottom-left) -->
          <div class="absolute bottom-4 left-4 right-4">
            <div class="inline-flex items-baseline gap-2 px-3 py-1.5 rounded-2xl bg-white/75 backdrop-blur-md shadow-sm">
              <h2 class="text-sm font-semibold text-slate-900">Silla de camping</h2>
              <span class="text-xs text-slate-700">• a 2 km de ti</span>
            </div>
          </div>
        </article>
      </div>
    </div>
  </div>
</div>

<style>
  /* --- Minimal extras for a polished landing-ready mockup --- */
  .phone-shadow { box-shadow: 0 30px 90px rgba(2,6,23,.25); }
  .card-shadow  { box-shadow: 0 20px 50px rgba(2,6,23,.25); }
  .no-select { -webkit-user-select: none; user-select: none; }

  /* Auto right-left "tease" swipe loop (no JS needed) */
  .auto-swipe {
    --like: 0;  /* green intensity (0..1) */
    --nope: 0;  /* red intensity (0..1) */
    will-change: transform, opacity;
    animation: autoSwipe 4s ease-in-out infinite;
  }
  @keyframes autoSwipe {
    0%   { transform: translate3d(0,0,0) rotate(0deg);      --like: 0; --nope: 0; }
    12%  { transform: translate3d(35px,-2px,0) rotate(6deg); --like: 1; --nope: 0; }
    30%  { transform: translate3d(0,0,0) rotate(0deg);      --like: 0; --nope: 0; }
    52%  { transform: translate3d(-35px,-2px,0) rotate(-6deg);--like: 0; --nope: 1; }
    70%  { transform: translate3d(0,0,0) rotate(0deg);      --like: 0; --nope: 0; }
    100% { transform: translate3d(0,0,0) rotate(0deg);      --like: 0; --nope: 0; }
  }

  /* Pastel overlays with centered labels; opacity driven by CSS vars */
  .overlay-like { opacity: var(--like); background: rgba(34,197,94,.25); mix-blend: normal; transition: opacity 150ms ease-out; }
  .overlay-nope { opacity: var(--nope); background: rgba(244,63,94,.25); mix-blend: normal; transition: opacity 150ms ease-out; }
  .label-like   { opacity: var(--like); transform: translateY(calc(8px * (1 - var(--like)))); transition: opacity 150ms ease-out, transform 150ms ease-out; }
  .label-nope   { opacity: var(--nope); transform: translateY(calc(8px * (1 - var(--nope)))); transition: opacity 150ms ease-out, transform 150ms ease-out; }
</style>
